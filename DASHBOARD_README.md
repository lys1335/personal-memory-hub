# Dashboard 测试面板

## 功能说明

这是一个简版的记忆检索测试面板，用于：
1. **对话测试**：输入问题，查询 Memory Hub 数据库中的记忆
2. **结果展示**：显示相关的记忆内容和相似度评分
3. **临时显示**：对话内容仅在当前会话中显示，不保存到数据库

## 使用方法

1. 确保 Memory Hub API 正在运行 (localhost:8000)
2. 打开 `dashboard-test.html` 文件
3. 在左侧输入问题，点击"发送"或按回车键
4. 右侧将显示检索到的相关记忆
5. 系统会自动生成基于记忆的回复

## 技术架构

- 前端：纯 HTML/CSS/JavaScript
- 后端接口：Memory Hub REST API
- 数据源：PostgreSQL + pgvector

## 界面布局

```
┌─────────────────────────────────────────────────────────────┐
│                      Dashboard 测试面板                       │
├─────────────────────────┬───────────────────────────────────┤
│                         │                                   │
│  💬 对话测试区          │  📚 记忆检索结果                   │
│                         │                                   │
│  [用户消息]             │  [记忆项1]                         │
│  [AI回复]               │    - 内容                          │
│                         │    - 相似度                        │
│  [输入框] [发送按钮]    │    - 来源                          │
│                         │                                   │
└─────────────────────────┴───────────────────────────────────┘
```

## 注意事项

- 此面板仅用于测试和展示目的
- 所有对话数据仅存储在浏览器内存中
- 不会修改或保存任何数据库记录
- 需要 Memory Hub API 正常运行才能使用
